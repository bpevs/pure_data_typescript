export const parseArrayChunk = jest.fn().mockReturnValue([])
export const parseCanvasChunk = jest.fn().mockReturnValue({})
export const parseElementChunk = jest.fn().mockReturnValue({})
export const parseUnknownChunk = jest.fn().mockReturnValue({})
